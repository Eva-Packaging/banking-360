name: PR Checks

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

jobs:
  validate-pr-title:
    name: Validate PR Title
    runs-on: ubuntu-latest
    steps:
      - name: Check PR title format
        env:
          PR_TITLE: ${{ github.event.pull_request.title }}
        run: |
          # Accepted formats (case-insensitive, anywhere in the title):
          #   BF3-123   BF3 123   bf3-123   bf3 123
          if echo "$PR_TITLE" | grep -qiE 'bf3[-[:space:]][0-9]+'; then
            echo "PR title is valid: $PR_TITLE"
          else
            echo "ERROR: PR title must contain a BF3 ticket reference."
            echo "Accepted formats (anywhere in the title, case-insensitive):"
            echo "  BF3-123   BF3 123   bf3-123   bf3 123"
            echo "Got: $PR_TITLE"
            exit 1
          fi
